22.16.0

# This file is use as the source of truth for the Node.JS version in the CI. Keeping it locked to a minor so the CI can fetch any patch version The source of truth for Node.JS in local is defined by the engines field in package.json (root) file.
